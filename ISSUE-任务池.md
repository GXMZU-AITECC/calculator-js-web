# Issue 任务池（供管理员照此在仓库开 Issue）

> 每个任务一个 Issue。开 Issue 后由成员认领（评论"认领"）。
> 标题严格使用 `feat: xxx` 格式，与 pr-rule.md 对齐。
> 【面试考核任务】标记的是面试考核用的入门任务。

---

## 1. feat: 渲染数字按键 0-9 【面试考核任务】

**目标**：在键盘容器中渲染出 0-9 十个数字按键（静态即可，暂不需要任何点击行为）。

**范围提示**：
- 改动只涉及 `index.html`（写静态按键）或 `js/view.js` 的 `renderKeyboard()`（JS 生成）二选一；
- 使用已预留的样式类 `.key .key--normal`，0 键用 `.key--wide` 占两列；
- 不要动 `css/style.css`、不要动其他 js 文件。

**验收标准**：浏览器打开 index.html，键盘区出现 10 个数字按键、布局整齐、风格与整体一致；PR 描述附截图。

---

## 2. feat: 渲染运算符与功能按键

**目标**：补齐 C（CLEAR）、退格（⌫）、+ - × ÷、. 、= 等按键，按 Python 版 view.py `setup_standard_buttons` 的布局摆放。

**范围提示**：样式类区分——CLEAR 用 `.key--danger`、运算符用 `.key--action`、等号用 `.key--success`。

---

## 3. feat: 用配置数组驱动按键渲染

**目标**：把按键的（文本/行列/样式）抽成一个配置数组，用循环渲染，消除重复 HTML（对应 Python 版 `_create_grid` 的思路）。

**范围提示**：这是重构任务——渲染结果必须和重构前完全一致。

---

## 4. feat: 数字按键点击追加到显示区

**目标**：点击数字键把对应数字追加到主屏末尾；主屏为 "0" 时替换而不是追加（对应 Python 版 controller 追加逻辑）。

**范围提示**：在 `renderKeyboard()` 或 `main.js` 里绑定点击事件，调 `controller.handleButtonClick(char)`；在 controller 里实现追加逻辑。

---

## 5. feat: CLEAR 清屏与退格

**目标**：C 键重置主屏为 "0" 并清空副屏；⌫ 键删除末尾一个字符，删空后显示 "0"。

---

## 6. feat: 小数点输入

**目标**：支持输入小数点；一个数字里不能出现两个小数点（参考 Python 版的防重复处理）。

---

## 7. feat: 四则运算（+ - * /）

**目标**：在 `model.js` 实现 `evaluate(expression)`，支持 + - * / 与运算优先级（× ÷ 先于 + -）。

**范围提示**：函数注释写清楚功能/参数/返回值（develop-rule 的代码风格要求）；不使用 `eval`，自己解析（这是本任务的考察点）。

---

## 8. feat: 等号求值显示结果

**目标**：按 = 时读取主屏表达式，调 `model.evaluate` 计算，结果写回主屏；表达式有错时主屏显示 Error、副屏提示（用 `formatter.formatResult` 格式化结果）。

---

## 9. feat: 运算符防重复

**目标**：连续输入多个运算符时忽略后一个；主屏为 "0" 时直接输运算符无效（对应 Python 版 controller 的运算符重复检查）。

---

## 10. feat: 233 彩蛋

**目标**：输入 233 按等号，主屏显示"哈哈哈"（致敬 Python 原版彩蛋）。低门槛任务，适合第一次提 PR 热身。

---

## 11. feat: 物理键盘输入支持

**目标**：支持键盘按键输入数字与运算符（keydown 监听），回车=等号、Backspace=退格、Esc=清屏。

---

## 12. feat: 千分位格式化

**目标**：在 `formatter.js` 新增千分位格式（如 1234567 → 1,234,567），结果显示时应用。

---

## 13. feat: 浮点结果去尾零

**目标**：完善 `formatter.formatResult`：6.0 → 6、0.30000000000000004 → 0.3（对应 Python 版的 round 处理）。

---

## 14. feat: 深色/浅色主题切换

**目标**：加主题切换按钮，用 CSS 变量实现两套配色。

---

## 远期候选（拆成 Issue 后再领）

- 括号支持、取模 %、正负号 +/-
- 计算历史记录（副屏显示）
- 程序员模式：进制切换 HEX/DEC/OCT/BIN（对标 Python 版）
- 时间模式：小时/分钟换算（对标 Python 版）
- 移动端适配（窗口 < 400px 的布局）
