/**
 * 控制器（Controller）：响应按键事件，协调 view 与 model。
 * 对应 Python 桌面版 controller.py。
 *
 * 待实现：按键点击处理 —— 见仓库 Issue 任务池。
 */
const controller = {
  /**
   * 处理按键点击（对应 Python 版 handle_button_click）。
   *
   * TODO(feat): 数字按键 → 将字符追加到显示区；
   * CLEAR / 退格 / 运算符 / 等号 的处理将按 Issue 任务池逐步添加。
   *
   * @param {string} char 被点击按键的字符，如 "7"、"+"
   * @returns {void}
   */
  handleButtonClick(char) {
    // 等待你的 PR
  },
};
